class Solution {
  public:
    int chocolates(int n, vector<int> &arr) {
        
        
        sort(arr.begin(),arr.end());
        
        for(int i=0;i<arr.size();i++){
            
            return arr[i];
        }
        
        
        
    }
    
    
};
